var markWeight = 78;
var markHeight = 1.69;

var johnWeight = 92;
var johnHeight = 1.95;

var mark_bmi = markWeight / (markHeight ** 2) ;

var john_bmi = johnWeight / (johnHeight ** 2);

console.log (mark_bmi > john_bmi);

//-----------------------------------------------------------------

markWeight = 95;
markHeight = 1.88;

johnWeight = 85;
johnHeight = 1.76


mark_bmi = markWeight / (markHeight ** 2) ;
john_bmi = johnWeight / (johnHeight ** 2);

 
console.log (mark_bmi > john_bmi);



 