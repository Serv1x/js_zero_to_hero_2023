const kianArray = [
    'Kian',
    'Schmedtmann',
    2037 - 1991,
    'teacher',
    ['Alicem', 'Neil','Leo']
    
];

for(let i = 0; i < kian.length; i++ ){
    console.log(kian[i], typeof kian[i]);

    types.push(typeof kian [i]);
}
console.log(ages);

console.log('--- Only Strings ---')
for(let i = 0; i < kian.length; i++ ){
    if(typeof kian[i] !== 'string') continue;

    console.log(jonas[i],typeof kian[i]);
}

console.log('--- Break with number ---')
for(let i = 0; i < kian.length; i++ ){
    if(typeof kian[i] === 'number') break;

    console.log(jonas[i],typeof kian[i]);
}